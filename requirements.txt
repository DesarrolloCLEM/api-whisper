# FastAPI y servidor
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Validación y configuración (pydantic >=2.12 para compatibilidad con Python 3.14)
pydantic>=2.12.0,<3
pydantic-settings>=2.2.0,<3

# Whisper y procesamiento de audio (faster-whisper con CTranslate2)
# faster-whisper es mucho más rápido que openai-whisper y soporta cuantización
faster-whisper>=1.0.0
ctranslate2>=4.0.0
huggingface-hub>=0.21
tokenizers>=0.13
# onnxruntime es opcional para el filtro VAD. Si no está instalado, el VAD se desactiva automáticamente.

# Base de datos (solo para API keys; SQLAlchemy >=2.0.41 para Python 3.14)
sqlalchemy>=2.0.41,<2.1
alembic>=1.12.1

# Text-to-Speech (TTS)
gtts>=2.5.0
elevenlabs>=1.0.0  # Para ElevenLabs TTS (opcional, solo si TTS_PROVIDER=elevenlabs)

# Utilidades
python-dotenv==1.0.0
pydub>=0.25.1
# Python 3.13+ eliminó audioop; pydub lo necesita para conversión
audioop-lts>=0.2.0; python_version >= "3.13"
